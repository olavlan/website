# Website-specific instructions

To debug in the client, use chrome-devtools and navigate to localhost:1234. Important: you cannot run `just serve` as a background task, so you have to ask the user to start the server if it's not running already.
